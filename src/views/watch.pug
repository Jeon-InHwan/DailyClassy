extends base.pug

block content
    div 
        h3
            a(href=`/posts/${post._id}`)=post.title
        p=post.content
        small=post.createdAt
    div 
        a(href=`${post.id}/edit`) Edit Post &rarr;
        
    

